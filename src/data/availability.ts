/**
 * Shown in the Hero's live-status badge. Update this as capacity changes,
 * it's kept out of Hero.tsx so it doesn't require touching layout code.
 */
export const availabilityStatus = 'Available for new projects'
